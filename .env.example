# Copy to .env locally. On Vercel, add the same keys in
# Project → Settings → Environment Variables (Production + Preview + Development).

# Dedicated HR Supabase project (schema "hr"). Region: ap-southeast-1 (Singapore).
# Vercel cannot reach db.*.supabase.co:5432 (IPv6-only). Use the Singapore pooler.

DATABASE_URL="postgresql://postgres.hmzlplfvaphyhgmmiryl:YOUR_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?sslmode=require&pgbouncer=true&connection_limit=1&pool_timeout=20&connect_timeout=15"
DIRECT_URL="postgresql://postgres.hmzlplfvaphyhgmmiryl:YOUR_PASSWORD@aws-0-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require"

# Optional — same as ecommerce backend / Vercel Supabase integration
POSTGRES_PRISMA_URL=
POSTGRES_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=postgres.hmzlplfvaphyhgmmiryl
POSTGRES_HOST=aws-0-ap-southeast-1.pooler.supabase.com
POSTGRES_PASSWORD=
POSTGRES_DATABASE=postgres

SUPABASE_REGION="ap-southeast-1"
JWT_SECRET="change-this-to-a-long-random-secret"
JWT_EXPIRES_IN="7d"
PORT=5001
CLIENT_URL="https://kisanmall-hr.vercel.app,http://localhost:5173,https://localhost:5173"
