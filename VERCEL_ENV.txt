# Vercel → kisanmall-hr-backend → Settings → Environment Variables
# Production + Preview + Development, then Redeploy.
# Paste the EXACT URI from Supabase → Connect. Do not invent a pooler username.

DATABASE_URL=postgresql://postgres:kisanmall123@db.hmzlplfvaphyhgmmiryl.supabase.co:5432/postgres?sslmode=require
DIRECT_URL=postgresql://postgres:kisanmall123@db.hmzlplfvaphyhgmmiryl.supabase.co:5432/postgres?sslmode=require
JWT_SECRET=kisan-mall-hr-dev-secret-change-in-production
CLIENT_URL=https://kisanmall-hr.vercel.app
SUPABASE_REGION=ap-south-1

# If login still fails with ENOTFOUND tenant/user:
# you pasted a pooler URI like postgres.hmzlplfvaphyhgmmiryl@aws-0-ap-south-1.pooler.supabase.com
# That tenant does not exist. Use the direct URI above, or copy Session pooler
# from the Supabase dashboard (do not guess the host).
